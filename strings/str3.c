/*find if the 2 given strings are anagrams

ex:pine,nipe are anagram
   hello,lheoo are not anagram */

#include<stdio.h>
#include<string.h>

int main()
{
  char str1[100];
  char str2[100];
  int len1;
  int len2;
  int num1;
  int num2;
  int i=0;
  int j=0;

  fgets(str1,100,stdin);
  fgets(str2,100,stdin);
  str1=[strlen(str1)-1]='\0';
  str2=[strlen(str2)-1]='\0';
  len1=strlen(str1);
  len2=strlen(str2);

   
}

  
  
